language: go
go:
  - release
env:
  - "PATH=/home/travis/gopath/bin:$PATH"
before_install:
  - go get github.com/mitchellh/gox
  - go get github.com/tcnksm/ghr
script:
  - echo "TODO TEST"
after_success:
  # Gox build and release
  - gox -output "dist/{{.OS}}_{{.Arch}}_{{.Dir}}"
  - ghr --username janritter --token $GITHUB_TOKEN --replace --prerelease --debug pre-release dist/